cubes:
- name: KPI_Lines
  extends: KPI_Lines_Base

  measures:

# Avg Lines per Invoice (#)

# Invoice Lines (#) / Invoices (#)

# - name: AVG_LINES_PER_INVOICE
#   sql: "CASE WHEN {KPI_Lines_Base.INVOICE_COUNT} > 0 THEN {KPI_Lines_Base.INVOICE_LINES_COUNT} / {KPI_Lines_Base.INVOICE_COUNT} ELSE 0 END"
#   type: number
